---
- hosts: bjorn
	tasks:
  - name: Add Docker Repository
    apt_repository:
      repo: deb [arch=amd64] https://download.docker.com/linux/ubuntu bionic stable
      state: present

	- name: Install Docker
		apt:
		 name: Docker
		 state: latest
		 update_cache: yes
